{
  "name": "photo-shoot-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude-compatible MCP photography trip planner",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "pack:mcpb": "npm run build && mcpb pack ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "astronomy-engine": "^2.1.19",
    "suncalc": "^1.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/suncalc": "^1.9.2",
    "typescript": "^5.8.3"
  }
}
